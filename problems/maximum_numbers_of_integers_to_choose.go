package problems 

func maxCount(banned []int, n int, maxSum int) int {
    m := make(map[int]bool)
    for k := range banned {
        m[k] = true
    }

    // numChosen := 0
    // currentSum := 0

    
    return 0
}
